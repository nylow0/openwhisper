[project]
name = "openwhisper-asr-service"
version = "0.1.0"
description = "OpenWhisper ASR worker integration wrapper"
requires-python = ">=3.10,<3.12"
dependencies = [
    "openwhisper-asr",
]

[tool.uv]
package = false

[tool.uv.sources]
openwhisper-asr = { path = "../../openwhisper-asr", editable = true }
