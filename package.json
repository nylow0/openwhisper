{
  "name": "openwhisper",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/desktop dev",
    "build": "bun run --cwd apps/desktop build",
    "typecheck": "bun run --cwd apps/desktop typecheck",
    "test": "bun run --cwd apps/desktop test",
    "check": "bun run --cwd apps/desktop typecheck && cargo check --manifest-path crates/openwhisper-native/Cargo.toml --target x86_64-pc-windows-msvc && cargo test --manifest-path crates/openwhisper-asr-rs/Cargo.toml --target x86_64-pc-windows-msvc",
    "cargo:build": "cargo build --manifest-path crates/openwhisper-native/Cargo.toml --target x86_64-pc-windows-msvc",
    "cargo:dev": "cargo run --manifest-path crates/openwhisper-native/Cargo.toml --target x86_64-pc-windows-msvc",
    "asr:rust:test": "cargo test --manifest-path crates/openwhisper-asr-rs/Cargo.toml --target x86_64-pc-windows-msvc",
    "setup": "bun install && cargo fetch"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
